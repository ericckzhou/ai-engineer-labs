# IMPLEMENTATION.md — Elective 06: Structured Output & Reliability

> [learner] Implementation notes. Fill as you build `structured_output.py`.

## Approach
*How did you structure extraction (balanced-brace scan?), validation (schema walk), and the coerce loop?*

## Key decisions
- **Extraction strategy:**
- **Validation error format (what strings do you return?):**
- **Repair prompt shape:**
- **Budget (`max_attempts`) + fail-closed behavior:**

## The bool/int trap
*How did you order the type checks so `True` is rejected where an int is required?*

## What I'd do differently

## Time spent
| Milestone | Hours |
|-----------|-------|
| M1 parse_strict | |
| M2 extract_json | |
| M3 validate | |
| M4 coerce loop | |
| M5 measure | |
