# Elective 03 — Cost & Latency Engineering
# The core runs OFFLINE (deterministic cheap/strong models + local embedder). A provider is only
# needed for the live extensions (real models / live prompt caching).

# USE_OLLAMA=1
# GROQ_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Map the cascade tiers to real models (live extension)
# CHEAP_MODEL=groq/llama-3.1-8b-instant
# STRONG_MODEL=groq/llama-3.3-70b-versatile
