# IMPLEMENTATION.md — Elective 02

> Document your build as you go: decisions, dead ends, and what each milestone taught you.

## M1 — scan_input
- Rules I implemented:
- Benign false positives I had to tune out:
- How I made it fail closed:

## M2 — redact_output
- Recognizers (regex + checksum):
- Operator choice (mask/replace/redact) and why:
- Structure-preservation issue I hit:

## M3 — enforce_policy
- Checks (length / forbidden / allow-list):
- Where it fails closed:

## M4 — run_guarded
- Order and why a blocked input never reaches the agent:
- The fail-closed wrapper around the stages:

## Notable decisions
*(log engineering decisions in DECISIONS.md)*
